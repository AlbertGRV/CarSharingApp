@model ApplicationUser
@{
    ViewData["Title"] = "Мой профиль";
}

<div class="container py-4">
    <div class="row">
        <div class="col-md-12 mb-4">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Главная</a></li>
                    <li class="breadcrumb-item"><a asp-action="Index">Личный кабинет</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Мой профиль</li>
                </ol>
            </nav>
        </div>
    </div>

    <div class="row">
        <div class="col-md-4">
            <div class="card mb-4">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0">Профиль пользователя</h5>
                </div>
                <div class="card-body text-center">
                    <div class="display-1 mb-3">👤</div>
                    <h3>@Model.UserName</h3>
                    <p class="text-muted">@Model.Email</p>
                    <p>Дата регистрации: @Model.CreatedAt.ToString("dd.MM.yyyy")</p>
                </div>
            </div>

            <div class="card">
                <div class="card-header bg-secondary text-white">
                    <h5 class="mb-0">Быстрые действия</h5>
                </div>
                <div class="card-body">
                    <div class="d-grid gap-2">
                        <a asp-area="Identity" asp-page="/Account/Manage/Index" class="btn btn-outline-primary">Настройки аккаунта</a>
                        <a asp-area="Identity" asp-page="/Account/Manage/ChangePassword" class="btn btn-outline-primary">Изменить пароль</a>
                        <a asp-action="Bookings" class="btn btn-outline-primary">Мои бронирования</a>
                        <a asp-controller="Cars" asp-action="Index" class="btn btn-primary">Забронировать автомобиль</a>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-8">
            <div class="card mb-4">
                <div class="card-header bg-success text-white">
                    <h5 class="mb-0">Личные данные</h5>
                </div>
                <div class="card-body">
                    <form>
                        <div class="mb-3">
                            <label class="form-label">Имя пользователя</label>
                            <input type="text" class="form-control" value="@Model.UserName" readonly>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Email</label>
                            <input type="email" class="form-control" value="@Model.Email" readonly>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Номер телефона</label>
                            <input type="tel" class="form-control" value="@Model.PhoneNumber" readonly>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Дата регистрации</label>
                            <input type="text" class="form-control" value="@Model.CreatedAt.ToString("dd.MM.yyyy HH:mm")" readonly>
                        </div>
                        <div class="alert alert-info">
                            Для изменения личных данных перейдите в <a asp-area="Identity" asp-page="/Account/Manage/Index">настройки аккаунта</a>
                        </div>
                    </form>
                </div>
            </div>

            <div class="card">
                <div class="card-header bg-warning text-dark">
                    <h5 class="mb-0">Водительские данные</h5>
                </div>
                <div class="card-body">
                    <div class="alert alert-warning">
                        <strong>Внимание!</strong> Для бронирования автомобилей необходимо заполнить водительские данные.
                    </div>
                    <form>
                        <div class="mb-3">
                            <label class="form-label">Номер водительского удостоверения</label>
                            <input type="text" class="form-control" placeholder="Введите номер ВУ">
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Дата выдачи</label>
                            <input type="date" class="form-control">
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Срок действия</label>
                            <input type="date" class="form-control">
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Категории</label>
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" id="categoryB">
                                <label class="form-check-label" for="categoryB">B - Легковые автомобили</label>
                            </div>
                        </div>
                        <div class="d-grid">
                            <button type="submit" class="btn btn-warning">Сохранить данные</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>